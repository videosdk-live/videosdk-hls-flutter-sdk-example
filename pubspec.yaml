name: videosdk_hls_flutter_example
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  videosdk: ^1.2.1
  http: ^01.2.2
  flutter_dotenv: ^5.0.2
  flutter_svg: ^2.0.2
  touch_ripple_effect: ^2.2.3
  flutter_vlc_player: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env
